# Main environment ("mia") -- used for everything EXCEPT DP-SGD training:
# model training (non-DP), DIME/baseline evaluation, figure generation.
# Deliberately excludes opacus -- kept in a separate environment
# (requirements-dpsgd.txt) to avoid a dual-cuDNN conflict between opacus
# and this torch build.
#
# PyTorch requires the CUDA-specific extra index; install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121

torch==2.5.1+cu121
torchvision==0.20.1+cu121
numpy
scikit-learn
matplotlib
Pillow